/*
 * generated by Xtext 2.21.0
 */
package org.xtext.example.mydsl.validation;

import org.eclipse.xtext.validation.Check;
import org.xtext.example.mydsl.stateMachine.StateMachine;
import org.xtext.example.mydsl.stateMachine.StateMachinePackage;

/**
 * This class contains custom validation rules. 
 *
 * See https://www.eclipse.org/Xtext/documentation/303_runtime_concepts.html#validation
 */
public class StateMachineValidator extends AbstractStateMachineValidator {
	
	public static final String INVALID_NAME = "invalidName";
	public static final String INVALID_ACTION = "invalidAction";
	private State event = new State();
	private State state = new State();
	private State instructions = new State();
	boolean empty = true

	
	@Check
	public void checkBlockIsNotEmpty(StateMachine model) {
		if(model.getEvent().isEmpty() || model.getInstructions().isEmpty() || model.getState().isEmpty()) {
			switch ()
			error("Empty blocks is not allowed!!", StateMachinePackage.Literals.STATE_MACHINE__EVENT, INVALID_ACTION);
		}
	}
	

	
//	public void checkForEmptyState(StateMachine model) {
//		if(model.getState() instanceof State) {
//			if(model.getState().isEmpty()) {
//				error("States needs to be defined for a StateMachine!", StateMachinePackage.Literals.STATE_MACHINE__STATE, INVALID_ACTION);
//			}
//		}
//			
//		
//	}
	
	
//
//	@Check
//	public void checkGreetingStartsWithCapital(Greeting greeting) {
//		if (!Character.isUpperCase(greeting.getName().charAt(0))) {
//			warning("Name should start with a capital",
//					StateMachinePackage.Literals.GREETING__NAME,
//					INVALID_NAME);
//		}
//	}
	
}
